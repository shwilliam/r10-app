import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  circularImage: {width: 100, height: 100, borderRadius: 50},
  smallImage: {
    width: 70,
    height: 70,
  },
})

export default styles
